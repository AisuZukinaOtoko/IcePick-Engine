#pragma once
#include <vector>

namespace IcePick {

	template<typename T>
	struct KeyFrame {
		T Value;

	};

	struct TimelineChannel {
		
	};

	class Timeline {
	public:
		Timeline();

	private:

	};
}